{
  "name": "servlets-frontend",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "scripts": {
    "build": "node build.js",
    "build:prod": "node build.js",
    "watch": "node build.js --watch"
  }
}
